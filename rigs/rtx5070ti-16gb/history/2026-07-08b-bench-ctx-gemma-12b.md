## 📍 Статус — чекпоинт 2026-07-08 (2): НОВЫЙ МЕТОД БЕНЧА — свип по глубине контекста + добавлена Gemma 4 12B QAT ✅

> **⚠️ КАК ТЕПЕРЬ ТЕСТИМ СКОРОСТЬ (замена старому «3 промпта ~6К × 3»).** Старый бенч мерил только на ~6К → завышал: decode на реально рабочих 32–60K заметно ниже. Теперь гоняем **свип по глубине контекста** — pp/tg на нескольких депсах (~2K/8K/32K/60K, для qwen +96K). Это ровно то, что делает стандартный `llama-bench -d <depths>` (комьюнити-стандарт), но через **живой `/completion` по llama-swap** (`cache_prompt=false`, `ignore_eos`, `n_predict=96`, temp 0, прогрев на каждом размере, 3 итерации) — цифры = прод 1:1.
>
> **Скрипт: `llama-swap/bench-ctx.py`** — запускается **С МАКБУКА** (`python3 bench-ctx.py [model_id ...]`), бьёт `http://$RIG_HOST:8080/upstream/<model_id>/completion`, VRAM снимает по SSH. Сырые числа → `bench-ctx-results.json`. Модели/глубины — словари `MODELS`/`SIZES` в шапке (добавить модель = строка в `MODELS`). `/upstream/<id>/...` с макбука доступен (200). Старый `bench-swap.ps1` (PC-side, только ~6К) оставлен как есть.
>
> **Результаты свипа (decode tg tok/s, замерено 2026-07-08):**
>
> | Глубина | gemma-4-12b-qat | qwen3.6-35b-a3b | Δ gemma |
> |---|---|---|---|
> | ~2K | 87.8 | 64.4 | +36% |
> | ~8K | 85.7 | 70.4 | +22% |
> | ~32K | 77.0 | 62.7 | +23% |
> | ~60K | 68.4 | 61.5 | +11% |
> | ~96K | — | 55.8 | — |
>
> prefill pp: gemma 3991→4105→3529→2914 (2K→60K); qwen 2750→3126→3108→2924→2686 (2K→96K). VRAM константа по глубине (KV на весь `-c` аллоцируется при загрузке): gemma **8990**, qwen **14852**. **Вывод:** gemma быстрее везде, но отрыв тает с контекстом (decode −22% к 60K — dense-внимание, KV растёт); qwen пологая (30/40 слоёв Gated DeltaNet = рекуррентное состояние фикс-размера, decode −21% лишь к 96K). Мелочь: у qwen 2K (64.4) < 8K (70.4) — на крохотном контексте не амортизируются оверхеды MoE/подтяжки экспертов из RAM (шум).

> **➕ Добавлена модель `gemma-4-12b-qat` в llama-swap** (по просьбе юзера — «скачать и попробовать»). **QAT** = quantization-aware training: 4-битный чекпоинт с качеством near-BF16 (в отличие от обычного post-training-кванта); один GGUF `UD-Q4_K_XL`. Модель **dense 12B** (не MoE) → целиком на GPU, без `--n-cpu-moe`. Файлы на ПК `C:\llm\models\gemma4-12b-qat\` (все байт-точно с HF): `gemma-4-12B-it-qat-UD-Q4_K_XL.gguf` (6.72 ГБ), `mtp-gemma-4-12B-it.gguf` (253 МБ, драфтер про запас), `mmproj-F16.gguf` (175 МБ — **зрение по просьбе юзера УБРАНО из конфига**, файл на диске мёртвый). Конфиг-блок: `-c 65536` (изначально пробовали 128K → пик VRAM 14852, впритык; на 64K пик **8990**, запас ~7.3 ГБ), сэмплинг Google `--temp 1.0 --top-p 0.95 --top-k 64`, макрос `${server}` (fa on, KV q8_0, `-ngl 999`, `--jinja`), `-ub 2048`. Бэкап прежнего блока (со зрением + 128K) — `config.yaml.bak` на ПК.
>
> **⚠️ Мышление Gemma включено по умолчанию** → на голый запрос уходит думать в `reasoning_content`, при упоре в `max_tokens` `content` пустой (`finish=length`). Прямой ответ: `"chat_template_kwargs":{"enable_thinking":false}` (проверено: `17*24`→`408`, stop). Текст и (когда было включено) зрение проверены end-to-end.
>
> **Про «idle VRAM 529 MiB» — не баг:** WDDM на этом ПК вытесняет VRAM простаивающего процесса в RAM и подгружает при запросе (уже описано в чекпоинте 2026-07-01). `nvidia-smi` в простое показывает ~529, во время генерации — реальный пик. Дашборд llama-swap (`http://$RIG_HOST:8080/ui`) при этом здоров.
